# _schema.yml for "revealjs-comic" format extension
# Describes format-specific options for IDE tooling and runtime validation.

$schema: https://m.canouil.dev/quarto-wizard/assets/schema/v2/extension-schema.json

# Per-format schemas.
# Options configured under format: revealjs-comic: in YAML.
formats:
  revealjs-comic:
    example-option:
      type: string
      description: "An example option for the revealjs-comic format."
